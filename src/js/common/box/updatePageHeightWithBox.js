import Tween from 'gsap';

export const updatePageHeightWithBox = () => {
	// const boxes = document.querySelectorAll('.box');
	// const wrapper = document.querySelector('#wrapper');
	// let maxHeight = 0;
	// /*
	// Get max box height
	//  */
	// for (let i = 0; i < boxes.length; i++) {
	// 	let height = boxes[i].getBoundingClientRect().height;
	// 	height > maxHeight ? maxHeight = height : null;
	// }
	// /*
	// Detect different between window&box height
	//  */
	// if (maxHeight > wrapper.getBoundingClientRect().height) {
	// 	Tween.set(wrapper, {
	// 		height: maxHeight + 550,
	// 	});
	// }
};